apiVersion: v1
kind: Service
metadata:
  name: finops-api
  labels:
    app: finops-api
    environment: staging
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: finops-api
    environment: staging
